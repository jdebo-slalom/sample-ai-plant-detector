# AWS Configuration
VITE_AWS_REGION=us-east-1

# Option 1: Use explicit credentials (works in browser)
VITE_AWS_ACCESS_KEY_ID=your_access_key
VITE_AWS_SECRET_ACCESS_KEY=your_secret_key

# Optional: Session token for temporary credentials (STS/SSO)
VITE_AWS_SESSION_TOKEN=your_session_token

# Option 2: Leave credentials empty to use default AWS credential chain
# Note: This only works with a backend proxy, not in browser

VITE_BEDROCK_MODEL_ID=anthropic.claude-3-sonnet-20240229-v1:0
